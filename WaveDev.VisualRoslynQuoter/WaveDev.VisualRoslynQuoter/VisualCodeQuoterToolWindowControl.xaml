<UserControl x:Class="WaveDev.VisualRoslynQuoter.VisualCodeQuoterToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Foreground="Black" Grid.Row="0" Padding="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="LightGray" TextWrapping="Wrap" Text="{Binding HintText}" />
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Command="{Binding PasteCommand}" Background="Transparent">
                <Button.Content >
                    <Image Source="F:\\Git Repositories\\VisualRoslynQuoter\\WaveDev.VisualRoslynQuoter\\WaveDev.VisualRoslynQuoter\\Resources\\Copy.png"/>
                </Button.Content>
            </Button>
        </StackPanel>
        <TextBox Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" Text="{Binding QuotedCode}"/>
    </Grid>
</UserControl>
